{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.description) }}
    {{ form_row(form.uploadedPic) }}
    {{ form_row(form.category) }}
    {{ form_row(form.department) }}
    <button class="btn btn-primary float-left">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}